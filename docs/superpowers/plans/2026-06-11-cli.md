# LLM Benchmark Platform — CLI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Python CLI (`benchmark`) with 4 command groups (model, task, report, status) that call the FastAPI backend over HTTP and display results in a human-readable table format.

**Architecture:** Click for command parsing. Rich for table/panel output. httpx for HTTP calls. Installed as `benchmark` via `pip install -e .`. Base URL defaults to `http://localhost:8080` but is overridable via `--url` or `BENCHMARK_URL` env var.

**Tech Stack:** Python 3.11+, Click, Rich, httpx, pytest

---

## File Map

| File | Responsibility |
|------|----------------|
| `cli/setup.py` | Package setup for `pip install -e .` |
| `cli/benchmark_cli/__init__.py` | Empty |
| `cli/benchmark_cli/main.py` | Root Click group, `--url` option, context object |
| `cli/benchmark_cli/client.py` | HTTP client helpers (GET/POST/PATCH with error handling) |
| `cli/benchmark_cli/commands/model.py` | `benchmark model list/register/load/unload` |
| `cli/benchmark_cli/commands/task.py` | `benchmark task list/create/start` |
| `cli/benchmark_cli/commands/report.py` | `benchmark report list/generate/show` |
| `cli/benchmark_cli/commands/status.py` | `benchmark status` |
| `cli/tests/test_model_commands.py` | Unit tests with mocked HTTP client |
| `cli/tests/test_task_commands.py` | Unit tests with mocked HTTP client |

---

## Task 1: Package Setup

**Files:**
- Create: `cli/setup.py`
- Create: `cli/benchmark_cli/__init__.py`
- Create: `cli/benchmark_cli/main.py`

- [ ] **Step 1: Create cli/setup.py**

```python
from setuptools import setup, find_packages

setup(
    name="benchmark-cli",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "click>=8.1",
        "rich>=13.0",
        "httpx>=0.27",
    ],
    entry_points={
        "console_scripts": [
            "benchmark=benchmark_cli.main:cli",
        ],
    },
)
```

- [ ] **Step 2: Create benchmark_cli/main.py**

```python
import click
from benchmark_cli.commands import model, task, report, status


class Context:
    def __init__(self, url: str):
        self.url = url.rstrip("/")


@click.group()
@click.option("--url", default="http://localhost:8080", envvar="BENCHMARK_URL", show_default=True)
@click.pass_context
def cli(ctx: click.Context, url: str):
    """LLM Benchmark Platform CLI"""
    ctx.obj = Context(url)


cli.add_command(model.group, name="model")
cli.add_command(task.group, name="task")
cli.add_command(report.group, name="report")
cli.add_command(status.command, name="status")
```

- [ ] **Step 3: Install CLI in development mode**

```bash
cd cli
pip install -e .
```

- [ ] **Step 4: Verify install**

```bash
benchmark --help
```

Expected:
```
Usage: benchmark [OPTIONS] COMMAND [ARGS]...
  LLM Benchmark Platform CLI
Options:
  --url TEXT  [default: http://localhost:8080]
  --help      Show this message and exit.
Commands:
  model
  report
  status
  task
```

- [ ] **Step 5: Commit**

```bash
git add cli/
git commit -m "feat: CLI package scaffold"
```

---

## Task 2: HTTP Client Helper

**Files:**
- Create: `cli/benchmark_cli/client.py`

- [ ] **Step 1: Create benchmark_cli/client.py**

```python
import httpx
import sys
from rich.console import Console

console = Console(stderr=True)


def get(url: str, base_url: str) -> dict | list:
    try:
        r = httpx.get(f"{base_url}/api/v1{url}", timeout=30)
        r.raise_for_status()
        return r.json()
    except httpx.HTTPStatusError as e:
        console.print(f"[red]Error {e.response.status_code}:[/red] {e.response.text}")
        sys.exit(1)
    except httpx.RequestError as e:
        console.print(f"[red]Connection error:[/red] {e}")
        sys.exit(1)


def post(url: str, base_url: str, json: dict | None = None) -> dict:
    try:
        r = httpx.post(f"{base_url}/api/v1{url}", json=json, timeout=30)
        r.raise_for_status()
        return r.json()
    except httpx.HTTPStatusError as e:
        console.print(f"[red]Error {e.response.status_code}:[/red] {e.response.text}")
        sys.exit(1)
    except httpx.RequestError as e:
        console.print(f"[red]Connection error:[/red] {e}")
        sys.exit(1)


def patch(url: str, base_url: str, json: dict) -> dict:
    try:
        r = httpx.patch(f"{base_url}/api/v1{url}", json=json, timeout=30)
        r.raise_for_status()
        return r.json()
    except httpx.HTTPStatusError as e:
        console.print(f"[red]Error {e.response.status_code}:[/red] {e.response.text}")
        sys.exit(1)
    except httpx.RequestError as e:
        console.print(f"[red]Connection error:[/red] {e}")
        sys.exit(1)
```

- [ ] **Step 2: Commit**

```bash
git add cli/benchmark_cli/client.py
git commit -m "feat: CLI HTTP client helper"
```

---

## Task 3: Model Commands

**Files:**
- Create: `cli/benchmark_cli/commands/__init__.py`
- Create: `cli/benchmark_cli/commands/model.py`
- Create: `cli/tests/__init__.py`
- Create: `cli/tests/test_model_commands.py`

- [ ] **Step 1: Write failing tests**

Create `cli/tests/test_model_commands.py`:

```python
import pytest
from unittest.mock import patch
from click.testing import CliRunner
from benchmark_cli.main import cli


@pytest.fixture
def runner():
    return CliRunner()


def test_model_list_displays_table(runner):
    mock_models = [{"id": 1, "name": "llama3", "model_path": "/models/llama3", "status": "active", "created_at": "2026-01-01T00:00:00"}]
    with patch("benchmark_cli.commands.model.api_get", return_value=mock_models):
        result = runner.invoke(cli, ["model", "list"])
    assert result.exit_code == 0
    assert "llama3" in result.output
    assert "active" in result.output


def test_model_register(runner):
    mock_model = {"id": 2, "name": "new-model", "model_path": "/p", "status": "inactive", "created_at": "2026-01-01T00:00:00"}
    with patch("benchmark_cli.commands.model.api_post", return_value=mock_model):
        result = runner.invoke(cli, ["model", "register", "new-model", "/p"])
    assert result.exit_code == 0
    assert "new-model" in result.output


def test_model_load(runner):
    mock_model = {"id": 1, "name": "m", "model_path": "/p", "status": "active", "created_at": "2026-01-01T00:00:00"}
    with patch("benchmark_cli.commands.model.api_patch", return_value=mock_model):
        result = runner.invoke(cli, ["model", "load", "1"])
    assert result.exit_code == 0
    assert "active" in result.output
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd cli
pytest tests/test_model_commands.py -v
```

Expected: `ImportError` (commands not yet created)

- [ ] **Step 3: Create commands/model.py**

```python
import click
from rich.table import Table
from rich.console import Console
from benchmark_cli.client import get as api_get, post as api_post, patch as api_patch

console = Console()


@click.group()
def group():
    """Model management commands"""


@group.command("list")
@click.pass_obj
def list_models(ctx):
    """List all registered models"""
    models = api_get("/models", ctx.url)
    table = Table(title="Registered Models")
    table.add_column("ID", style="cyan", width=6)
    table.add_column("Name", style="bold")
    table.add_column("Path", no_wrap=False)
    table.add_column("Status")
    for m in models:
        status_style = "green" if m["status"] == "active" else "dim"
        table.add_row(str(m["id"]), m["name"], m["model_path"], f"[{status_style}]{m['status']}[/{status_style}]")
    console.print(table)


@group.command("register")
@click.argument("name")
@click.argument("model_path")
@click.pass_obj
def register_model(ctx, name, model_path):
    """Register a new model (NAME MODEL_PATH)"""
    model = api_post("/models", ctx.url, json={"name": name, "model_path": model_path})
    console.print(f"[green]Registered:[/green] {model['name']} (id={model['id']})")


@group.command("load")
@click.argument("model_id", type=int)
@click.pass_obj
def load_model(ctx, model_id):
    """Set model status to active"""
    model = api_patch(f"/models/{model_id}/status", ctx.url, json={"status": "active"})
    console.print(f"[green]Loaded:[/green] {model['name']} → {model['status']}")


@group.command("unload")
@click.argument("model_id", type=int)
@click.pass_obj
def unload_model(ctx, model_id):
    """Set model status to inactive"""
    model = api_patch(f"/models/{model_id}/status", ctx.url, json={"status": "inactive"})
    console.print(f"[yellow]Unloaded:[/yellow] {model['name']} → {model['status']}")
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd cli
pytest tests/test_model_commands.py -v
```

Expected: all 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add cli/benchmark_cli/commands/ cli/tests/
git commit -m "feat: model CLI commands with tests"
```

---

## Task 4: Task Commands

**Files:**
- Create: `cli/benchmark_cli/commands/task.py`
- Create: `cli/tests/test_task_commands.py`

- [ ] **Step 1: Write failing tests**

Create `cli/tests/test_task_commands.py`:

```python
import pytest
from unittest.mock import patch
from click.testing import CliRunner
from benchmark_cli.main import cli


@pytest.fixture
def runner():
    return CliRunner()


def test_task_list(runner):
    mock_tasks = [{"id": 1, "name": "k-battle", "dimension": "knowledge", "status": "pending", "created_at": "2026-01-01T00:00:00", "completed_at": None}]
    with patch("benchmark_cli.commands.task.api_get", return_value=mock_tasks):
        result = runner.invoke(cli, ["task", "list"])
    assert result.exit_code == 0
    assert "k-battle" in result.output


def test_task_create(runner):
    mock_task = {"id": 2, "name": "new", "dimension": "code", "status": "pending", "created_at": "2026-01-01T00:00:00", "completed_at": None}
    with patch("benchmark_cli.commands.task.api_post", return_value=mock_task):
        result = runner.invoke(cli, ["task", "create", "new", "--dimension", "code", "--models", "1,2", "--rounds", "3"])
    assert result.exit_code == 0
    assert "new" in result.output


def test_task_start(runner):
    mock_task = {"id": 1, "name": "t", "dimension": "knowledge", "status": "running", "created_at": "2026-01-01T00:00:00", "completed_at": None}
    with patch("benchmark_cli.commands.task.api_post", return_value=mock_task):
        result = runner.invoke(cli, ["task", "start", "1"])
    assert result.exit_code == 0
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd cli
pytest tests/test_task_commands.py -v
```

Expected: `ImportError`

- [ ] **Step 3: Create commands/task.py**

```python
import click
from rich.table import Table
from rich.console import Console
from benchmark_cli.client import get as api_get, post as api_post

console = Console()


@click.group()
def group():
    """Task management commands"""


@group.command("list")
@click.pass_obj
def list_tasks(ctx):
    """List all evaluation tasks"""
    tasks = api_get("/tasks", ctx.url)
    table = Table(title="Evaluation Tasks")
    table.add_column("ID", width=6)
    table.add_column("Name")
    table.add_column("Dimension", width=12)
    table.add_column("Status", width=12)
    table.add_column("Created", width=20)
    status_colors = {"pending": "dim", "running": "yellow", "completed": "green", "failed": "red"}
    for t in tasks:
        color = status_colors.get(t["status"], "white")
        table.add_row(str(t["id"]), t["name"], t["dimension"], f"[{color}]{t['status']}[/{color}]", t["created_at"][:19])
    console.print(table)


@group.command("create")
@click.argument("name")
@click.option("--dimension", default="knowledge", type=click.Choice(["knowledge", "reasoning", "code", "safety"]), show_default=True)
@click.option("--models", default="", help="Comma-separated model IDs")
@click.option("--rounds", default=5, show_default=True)
@click.pass_obj
def create_task(ctx, name, dimension, models, rounds):
    """Create a new evaluation task"""
    model_ids = [int(x) for x in models.split(",") if x.strip()]
    task = api_post("/tasks", ctx.url, json={
        "name": name,
        "dimension": dimension,
        "task_type": "adversarial",
        "config": {"model_ids": model_ids, "rounds": rounds},
    })
    console.print(f"[green]Created:[/green] Task #{task['id']} '{task['name']}' ({task['dimension']})")


@group.command("start")
@click.argument("task_id", type=int)
@click.pass_obj
def start_task(ctx, task_id):
    """Start a pending evaluation task"""
    api_post(f"/tasks/{task_id}/start", ctx.url)
    console.print(f"[green]Started:[/green] Task #{task_id}")
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd cli
pytest tests/test_task_commands.py -v
```

Expected: all 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add cli/benchmark_cli/commands/task.py cli/tests/test_task_commands.py
git commit -m "feat: task CLI commands with tests"
```

---

## Task 5: Report & Status Commands

**Files:**
- Create: `cli/benchmark_cli/commands/report.py`
- Create: `cli/benchmark_cli/commands/status.py`

- [ ] **Step 1: Create commands/report.py**

```python
import click
import json
from rich.table import Table
from rich.panel import Panel
from rich.console import Console
from benchmark_cli.client import get as api_get, post as api_post

console = Console()


@click.group()
def group():
    """Report commands"""


@group.command("list")
@click.pass_obj
def list_reports(ctx):
    """List all reports"""
    reports = api_get("/reports", ctx.url)
    table = Table(title="Reports")
    table.add_column("ID", width=6)
    table.add_column("Model ID", width=10)
    table.add_column("Type", width=12)
    table.add_column("Generated", width=20)
    for r in reports:
        table.add_row(str(r["id"]), str(r["model_id"]), r.get("report_type", ""), r["generated_at"][:19])
    console.print(table)


@group.command("generate")
@click.argument("model_id", type=int)
@click.option("--type", "report_type", default="full", type=click.Choice(["full", "summary"]), show_default=True)
@click.pass_obj
def generate_report(ctx, model_id, report_type):
    """Generate a report for a model"""
    report = api_post("/reports", ctx.url, json={"model_id": model_id, "task_ids": [], "report_type": report_type})
    console.print(f"[green]Generated:[/green] Report #{report['id']} for model {model_id}")


@group.command("show")
@click.argument("report_id", type=int)
@click.pass_obj
def show_report(ctx, report_id):
    """Show report details"""
    report = api_get(f"/reports/{report_id}", ctx.url)
    content_str = json.dumps(report.get("content", {}), indent=2, ensure_ascii=False)
    console.print(Panel(content_str, title=f"Report #{report_id}", expand=False))
```

- [ ] **Step 2: Create commands/status.py**

```python
import click
from rich.table import Table
from rich.console import Console
from benchmark_cli.client import get as api_get

console = Console()


@click.command()
@click.pass_obj
def command(ctx):
    """Show system status"""
    health = api_get("/system/health", ctx.url)
    status = api_get("/system/status", ctx.url)
    console.print(f"API: [green]{health['status']}[/green]")
    console.print(f"vLLM: [{'green' if status['vllm_status'] == 'online' else 'red'}]{status['vllm_status']}[/{'green' if status['vllm_status'] == 'online' else 'red'}]")
    if status.get("loaded_models"):
        table = Table(title="Loaded Models")
        table.add_column("Model")
        for m in status["loaded_models"]:
            table.add_row(m)
        console.print(table)
    else:
        console.print("[dim]No models currently loaded in vLLM[/dim]")
```

- [ ] **Step 3: Commit**

```bash
git add cli/benchmark_cli/commands/report.py cli/benchmark_cli/commands/status.py
git commit -m "feat: report and status CLI commands"
```

---

## Task 6: End-to-End CLI Verification

- [ ] **Step 1: Run all CLI tests**

```bash
cd cli
pytest tests/ -v
```

Expected: all tests PASS

- [ ] **Step 2: Verify CLI against running backend**

Start backend first:
```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8080
```

Then in another terminal:
```bash
benchmark status
benchmark model list
benchmark model register test-model /models/test
benchmark model list
benchmark task list
benchmark task create my-battle --dimension knowledge --models 1 --rounds 2
benchmark task list
benchmark report list
```

Expected: each command prints a rich table or success message without errors.

- [ ] **Step 3: Final commit**

```bash
git add cli/
git commit -m "feat: CLI complete — model/task/report/status commands"
```

---

## Self-Review Checklist

- [x] **model list** → Task 3
- [x] **model register** → Task 3
- [x] **model load/unload** → Task 3
- [x] **task list** → Task 4
- [x] **task create** → Task 4 (includes model_ids, rounds, dimension)
- [x] **task start** → Task 4
- [x] **report list** → Task 5
- [x] **report generate** → Task 5
- [x] **report show** → Task 5
- [x] **status** → Task 5
- [x] **--url / BENCHMARK_URL** → Task 1
- [x] **Error handling** → Task 2 (HTTP errors printed to stderr with sys.exit)
- [x] **Rich table output** → all commands
- [x] **Unit tests** → Tasks 3 & 4
